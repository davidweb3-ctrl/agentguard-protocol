# Scripts

Planned scripts:

- `create-test-mint.ts`: create a local or devnet test token that stands in for USDC.
- `seed-demo.ts`: create an agent, deposit tokens, and add a merchant policy.
- `agent-client.ts`: call the demo API, pay the 402 challenge, and retry.

These are intentionally left as the next implementation step after the Anchor program compiles.
